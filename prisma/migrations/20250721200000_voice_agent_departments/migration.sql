-- Department-scoped voice agent SKUs (SQLite/D1 stores ModuleId as TEXT).
-- No structural change required beyond documenting allowed values for new DealershipModule rows.
-- Existing voice_agent rows continue to gate the parent phone AI feature.

SELECT 1;
